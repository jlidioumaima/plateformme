<form method="post" action="{{ path('app_tarifaire_delete', {'id': grille_tarifaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" style="display: contents;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ grille_tarifaire.id) }}">
      <button class="btn btn-danger"  style="width:40px"><i class="icon-trash-alt"></i>  </button>

</form>
