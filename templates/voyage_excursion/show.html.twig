{% extends 'base.html.twig' %}

{% block title %}VoyageExcursion{% endblock %}

{% block body %}
    <h1>VoyageExcursion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ voyage_excursion.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ voyage_excursion.type }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ voyage_excursion.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_voyage_excursion_index') }}">back to list</a>

    <a href="{{ path('app_voyage_excursion_edit', {'id': voyage_excursion.id}) }}">edit</a>

    {{ include('voyage_excursion/_delete_form.html.twig') }}
{% endblock %}
